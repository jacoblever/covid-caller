{
  "name": "covid-caller",
  "version": "1.0.0",
  "description": "Covid Caller",
  "main": "index.js",
  "scripts": {
    "start": "sam build && sam local start-api --env-vars=.local-env.json",
    "deploy": "sam build && sam deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jacob Lever",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.814.0"
  }
}
